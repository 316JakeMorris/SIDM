#define some selections that will hopefully get us the signal region
#This is a combination of the &4mu and &base_ljObjCut_ljIso_2lj selections. However, there is no easy way to combine two selections, 
#ESPECIALLY if they are extremely nested and referential to other selections. Used a tool called yq to explode the selections.yaml
#and then I manually combined &4u and &base_ljObjCut_ljIso_2lj. Interestingly, the only thing I added was "mu_lj_iso < 0.1" and "egm_lj_iso < 0.2". 
#Exact command yq 'explode(.)' selections.yaml'
4mu_SR: &4mu_SR
  obj_cuts:
    genMus:
      - "status 1"
    genEs:
      - "status 1"
    electrons:
      - "pT > 10 GeV"
      - "|eta| < 2.4"
      - 'MVANonIsoWPL'
    muons:
      - "looseID"
      - "pT > 5 GeV"
      - "|eta| < 2.4"
    photons:
      - "pT > 20 GeV"
      - "|eta| < 2.5"
      - 'Custom Cutbased'
      - "pixelSeed"
      - 'Photon DR Veto 0p025'
    dsaMuons:
      - "pT > 10 GeV"
      - "|eta| < 2.4"
      - "displaced ID"
      - "all" # cross-cleaning
  postLj_obj_cuts:
    ljs:
      - "pT > 30 GeV"
      - "|eta| < 2.4"
      - "mu_charge == 0"
    mu_ljs:
      - "mu_lj_iso < 0.1"
      - "displaced"
    egm_ljs:
      - "egm_lj_iso < 0.2"
      - "displaced"
  evt_cuts:
    - - - - evt_cuts:
              - "pass triggers"
          - - "PV filter"
      - ">=2 LJs"
    - "4mu"
    # - ">=1 egm_ljs" #Could also apply a egm_ljs == 0 in order to get an 4mu region that is orthogonal to the 2mu2e region. 
    - "egm_ljs = 0"
    - ">=2 mu_ljs"
    # - "dPhi(LJ_0, LJ_1) > 2" #Done by mask until we get sequential event cuts